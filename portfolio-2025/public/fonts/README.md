Place the Doto font files here to enable the exact Figma typography.

Expected filenames (update if your font files use different names):
- Doto-ExtraBold.woff2
- Doto-ExtraBold.woff

After adding the files, start the dev server:

cd /Users/macbook/Documents/portfolio/portfolio-2025
npm run dev

The CSS already contains an @font-face rule that will load these files automatically.
